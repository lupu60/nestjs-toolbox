name: Code Formatting

on:
  push:
    branches:
      - master
      - main
  pull_request:
    branches:
      - master
      - main

jobs:
  format-and-lint:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Install Biome
      run: npm install -g @biomejs/biome

    - name: Format code
      run: |
        # First check files without formatting
        biome ci --diagnostic-level=warn packages
        
        # If that passes, do actual formatting
        find packages -type f \( -name "*.ts" -o -name "*.json" \) ! -path "*/node_modules/*" -print0 | xargs -0 biome format --write
      continue-on-error: true

    - name: Lint code
      run: |
        # Check linting without fixing
        biome ci --diagnostic-level=warn packages
      continue-on-error: true

    - name: Check for changes
      id: check_changes
      run: |
        git diff --exit-code
        echo "changes=$(git diff --exit-code && echo 'false' || echo 'true')" >> $GITHUB_OUTPUT

    - name: Commit formatted files
      if: steps.check_changes.outputs.changes == 'true'
      run: |
        git config --global user.name 'GitHub Actions'
        git config --global user.email 'actions@github.com'
        git add .
        git commit -m "Format and lint code using Biome [automated]"
        git push

    - name: Final linting check
      run: |
        biome ci packages